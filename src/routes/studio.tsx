import { createFileRoute } from "@tanstack/react-router";
import { GlobalAssistant } from "@/components/GlobalAssistant";
import { ImageStudio } from "@/components/ImageStudio";
import { studioInformation } from "@/lib/tool-information";
export const Route = createFileRoute("/studio")({ head: () => ({ meta: [{ title: "Studio AI — FormAI Studio" }, { name: "description", content: "Create photorealistic interiors using exact furniture reference images." }, { property: "og:title", content: "Studio AI" }, { property: "og:description", content: "Place exact referenced furniture into photorealistic interior renderings." }] }), component: () => <><GlobalAssistant /><ImageStudio planMode eyebrow="Studio AI" title="Design your space" description="Configure the room and upload exact furniture references—from your FormAI Studio library or any existing piece—to place them in the rendering with complete visual fidelity." suggestions={["Contemporary living room", "Parisian apartment", "Organic modern", "Bold and sculptural"]} information={studioInformation} /></> });
