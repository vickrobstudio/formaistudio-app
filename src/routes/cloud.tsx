import { createFileRoute, Link } from "@tanstack/react-router";
import { Box, FileImage, FolderOpen } from "lucide-react";
import { Button } from "@/components/ui/button";
import { BackLink, FormaHeader, PageIntro } from "@/components/FormaMobile";
export const Route = createFileRoute("/cloud")({ head: () => ({ meta: [{ title: "My Cloud — Forma Studio" }, { name: "description", content: "Your saved AI designs, products and materials." }, { property: "og:title", content: "My Cloud" }, { property: "og:description", content: "Access saved designs and creative assets." }] }), component: CloudPage });
function CloudPage() { return <main className="min-h-screen bg-background"><FormaHeader /><div className="px-5 pt-7"><BackLink /></div><PageIntro eyebrow="Personal library" title="My Cloud" description="Your saved projects, generated images, product references and materials appear here." /><section className="px-5 pb-16"><div className="grid grid-cols-3 gap-2">{[[FolderOpen,"Projects"],[FileImage,"Outputs"],[Box,"Products"]].map(([Icon,label]) => { const ItemIcon = Icon as typeof Box; return <div key={label as string} className="flex min-h-28 flex-col items-center justify-center gap-3 bg-secondary text-xs"><ItemIcon className="size-5" />{label as string}</div>; })}</div><div className="mt-12 text-center"><p className="text-lg font-light">Your cloud is empty</p><p className="mt-2 text-xs text-muted-foreground">Sign in and create your first design to see it here.</p><Button asChild variant="studio" className="mt-6"><Link to="/auth">Sign in</Link></Button></div></section></main>; }